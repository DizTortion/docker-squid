kind: pipeline
type: kubernetes
name: default

steps:
  - name: image
    image: plugins/docker
    settings:
      repo: registry.internal.hoffmeister.biz/diztortion/squid
      registry: registry.internal.hoffmeister.biz
      auto_tag: false
    when:
      branch:
        - master
